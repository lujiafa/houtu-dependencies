{
	"properties": [
		{
			"name": "houtu.feign.exception-source-trace",
			"type": "java.lang.Boolean",
			"description": "Whether to write service trace info (currently the source service name spring.application.name; may be extended to the full call chain) into the exception response header. When disabled, a placeholder value is used to avoid leaking internal service info.",
			"defaultValue": false
		}
	]
}
